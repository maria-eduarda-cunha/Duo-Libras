export const environment = {
  production: true,
  apiMemberCodespace: '/member/', // necessário para o uso no codespace: apagar e usar apenas localhost
  apiMember: '/member/',
  apiQuiz: '/quiz/',
};
